# Binaries
bin/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Dependency directories
vendor/

# Go workspace file
go.work

# Environment files (local overrides)
.env

# docker-compose.env is tracked (contains dev/example config)
# For production, create a separate file or use .env for overrides
